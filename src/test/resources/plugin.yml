name: MockAddon
version: MockVersion
main: io.github.mooy1.infinitylib.core.MockAddon

commands:
  mockaddon:
    description: MockAddon Command
  test:
    description: Test AddonCommand

permissions:
  test:
    default: op